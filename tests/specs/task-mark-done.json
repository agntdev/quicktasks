[
  {
    "name": "Mark Done shows error for missing task",
    "steps": [
      {
        "send": { "callback": "task:mark_done:nonexistent" },
        "expect": [
          { "method": "answerCallbackQuery" },
          {
            "method": "sendMessage",
            "payload": {
              "text": "That task no longer exists."
            }
          }
        ]
      }
    ]
  }
]
