[
  {
    "name": "Assign to me shows error for missing task",
    "steps": [
      {
        "send": { "callback": "task:assign_self:nonexistent" },
        "expect": [
          { "method": "answerCallbackQuery" },
          {
            "method": "sendMessage",
            "payload": {
              "text": "That task no longer exists."
            }
          }
        ]
      }
    ]
  }
]
